<% content_for :page_title, "Check and confirm your answers - Previous allegations" %>
<% content_for :back_link_url, polymorphic_path([:edit, current_referral.routing_scope, current_referral]) %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_with model: @previous_misconduct_check_answers_form, url: [current_referral.routing_scope, current_referral, :previous_misconduct, :check_answers], method: :patch do |f| %>
      <%= f.govuk_error_summary %>

      <h1 class="govuk-heading-l">
        <span class="govuk-caption-l">Previous allegations</span>
        Check and confirm your answers
      </h1>

      <%= render partial: 'referrals/shared/check_answers', locals: { f:, complete_field_name: :previous_misconduct_complete } %>
    <% end %>
  </div>
</div>
